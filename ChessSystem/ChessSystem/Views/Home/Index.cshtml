@Html.Partial("_Navbar")

<div class="col-md-10 col-md-offset-1">
    <h1>Home page</h1>
    
    @if (Session["Name"] != null)
    {
        <h3>Welcome, @Session["Name"].ToString()</h3>
    }
</div>